# Notebook → Code Mapping

This file maps the three provided notebooks to their corresponding implementation files in the repository, and lists observed mismatches or TODOs for reproducibility.

## notebooks/Haxby-visual-reconstruction.ipynb
- Data & preprocessing: maps to `src/fmri_reconstruction/data/haxby.py` and `src/fmri_reconstruction/preprocessing.py`
- Transformer model: `src/fmri_reconstruction/models/transformer.py`
- CLIP / DINO projectors: `src/fmri_reconstruction/models/projectors.py`
- Diffusion prior(s): `src/fmri_reconstruction/models/diffusion_prior.py`
- Retrieval DB & helpers: `src/fmri_reconstruction/retrieval/cosine_retrieval.py` (and `src/fmri_reconstruction/retrieval/database.py`)
- Evaluation & metrics: `src/fmri_reconstruction/evaluation/image_metrics.py`, `src/fmri_reconstruction/evaluation/retrieval_metrics.py`
- Stable Diffusion usage: notebook uses `diffusers` directly; repository has a stub at `src/fmri_reconstruction/reconstruction/stable_diffusion.py` (TODO: wrap pipeline)
- Notes: notebook loads dataset checkpoints from `multi_subject/*.pth` — these are external artifacts and must be produced by training scripts (missing / TODO).

## notebooks/LIVE-visual-reconstruction.ipynb
- Data adapter & preprocessing contract: `src/fmri_reconstruction/data/live.py` and `src/fmri_reconstruction/preprocessing.py`
- Transformer, projectors, priors: same model modules as Haxby (`models/transformer.py`, `models/projectors.py`, `models/diffusion_prior.py`)
- Dual-prior DINO branch: maps to `models/diffusion_prior.py` (repo contains dual-prior scaffolding/test coverage)
- Retrieval encoding & caching: `src/fmri_reconstruction/retrieval/database.py` / `retrieval/cosine_retrieval.py`
- Evaluation: `src/fmri_reconstruction/evaluation/*` (image + feature metrics)
- Stable Diffusion Img2Img: notebook directly calls `diffusers`; see stub `reconstruction/stable_diffusion.py` (TODO: implement wrapper to centralize safety_checker, dtype, device handling)
- Notes: Notebook expects dataset-specific checkpoints under a `CHECKPOINT_DIR` (dataset-local); training loop to create these checkpoints is not yet implemented in `src/fmri_reconstruction/training` (TODO).

## notebooks/NSD-visual-reconstruction.ipynb
- Data adapter & manifests: `src/fmri_reconstruction/data/nsd.py` and `src/fmri_reconstruction/preprocessing.py`
- Transformer, projectors, priors: `models/transformer.py`, `models/projectors.py`, `models/diffusion_prior.py`
- Retrieval, encoding: `retrieval/database.py`, `retrieval/cosine_retrieval.py`
- Evaluation: `evaluation/image_metrics.py`, `evaluation/feature_metrics.py`
- Notes: NSD notebook contains HDF5 inspection cells and explicit prepared-array contract — matches repo's preference for prepared shared-aligned arrays. Checkpoints again expected in dataset `CHECKPOINT_DIR` (training missing).

## Common mismatches & TODOs
- `reconstruction/stable_diffusion.py` is a stub; notebooks call `diffusers` pipelines directly. Add a wrapper to centralize loading, dtype/device, and safety rules.
- Training loop / checkpointing: notebooks assume dataset-specific `*_best.pth` checkpoints. Implement `src/fmri_reconstruction/training/trainer.py` loops (TODO: scheduled training, validation, AMP, checkpoint saving) and dataset-specific checkpoint layout.
- Checkpoint naming & locations: notebooks use `CHECKPOINT_DIR / "*_best.pth"`; standardize checkpoint paths and document in `README`.
- External weights & datasets: notebooks load precomputed `.pth` files and image manifests outside the repo. Add clear documentation and verification in `AUDIT/FINAL_VALIDATION.md` about missing external assets.
- Retrieval manifest safety: ensure retrieval manifests use training images only (noted in notebooks). Add an automated split-guard test to verify manifests before building DB.

## Next suggested steps
- Implement `reconstruction/stable_diffusion.py` wrapper and replace direct `diffusers` calls in notebooks with `fmri_reconstruction.reconstruction` API.
- Implement training loop that saves the expected checkpoint names and add small smoke-run unit tests that create tiny checkpoints and verify notebook flows.
- Add CI smoke workflow that runs a minimal reconstruction pipeline using synthetic data to validate end-to-end notebook code paths.

---
Generated by the repository audit step on 2026-08-25.
